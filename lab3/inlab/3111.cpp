#include <iostream>
using namespace std;

int* flatten(int** matrix, int r, int c) {
    // int* newArr = new int[r*c];
    // int index = 0;
    // for (int a = 0; a < r; a++) {
    //     for (int b = 0; b < c; b++) {
    //         newArr[index] = matrix[a][b];
    //         index++;
    //     }
    // }
    // return newArr;

    int* newArr = new int[r * c];
    for (int i = 0; i < r + c; i++) {
        // newArr[i] = matrix[1];
    }
}